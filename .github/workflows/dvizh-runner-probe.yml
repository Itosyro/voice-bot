name: DVIZH runner probe

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/dvizh-runner-probe.yml'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Verify hosted runner
        run: echo runner-ok
